enum Role {
  ADMIN
  USER
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique @map("email_address")
  role      Role?
  posts     Post[]
  createdAt DateTime @default(now())

  @@map("users")
}
